name: Matrix
on: [push]

jobs:
  node-matrix:
    strategy:
        matrix:
          os: [ubuntu-latest, windows-latest]
          node-versions: [13, 14, 15]
    runs-on: ${{ matrix.os }}
    steps:
      - run: node -v
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-versions }}
      - run: node -v

